C:\Users\wadith.USFAAD-2G3S7J4\Documents\chivo\robot-dog-stairs\external\unitree_rl_mjlab\src\tasks\velocity\mdp\rewards.py:376: 
RuntimeWarning: get_terrain_height_at_foot_positions: falling back to env_origins[:, 2]. 
This is the tile origin z, not the height under each foot — clearance will be wrong wherever terrain height varies within a tile (e.g. stairs).Expose height_field_raw + horizontal_scale on your terrain entity. 
terrain_z = get_terrain_height_at_foot_positions(env, foot_xy) # [B, N]

It means your terrain-relative feet_clearance is still not truly working on stairs. Training can continue, but the foot-clearance reward is not accurate. 
The robot may still learn from other rewards, but for better stair learning, you need to expose/query real terrain height under each foot.

Fix: Use the height scan point closest to each foot, or at least use the terrain scan around the base as an estimate. But the cleaner version is: add a dedicated foot raycast sensor that casts downward from each foot.

mjlab terrain is not a heightfield raster. It generates terrain as actual MuJoCo geometry, so your old fallback chain looking for height_field_raw, horizontal_scale, etc. will not work. The uploaded investigation shows TerrainEntity only exposes things like env_origins, terrain_origins, and flat_patches, not a real height-query API.

So the correct long-term fix is:

Use raycasting against the real MuJoCo terrain geometry.



#### refine phase 3:
Reason: oh wait one more thing, compared to the first training, now the dog robot walk not as smooth, there 4 legs looks like not cooperating with each other as it was, i mean it can still working but the rythm is weird maybe it was the problem you stated?: Velocity tracking degraded

Stage 1:

error_vel_xy: 0.5982

Stage 3:

0.9263

Yaw:

1.0091

This is significantly worse.

The robot is basically saying:

"I know how to survive and climb stairs, but I am not very good at following commands anymore."

This often happens on stairs.

The policy starts prioritizing:

don't fall
clear steps
stay balanced

and only then:

obey velocity commands
Action rate penalty increased

Stage 1:

action_rate_l2: -1.7783

Stage 3:

-2.0717

This means:

the policy is moving joints more aggressively.

This is not necessarily bad.

Stairs require:

lifting
searching
correcting footholds
pushing upward

All of these need more aggressive motions.

cfg.rewards["foot_gait"].weight = 0.8      # from 0.5
cfg.rewards["action_rate_l2"].weight = -0.08  # from -0.05
cfg.rewards["foot_slip"].weight = -0.35    # from -0.25

twist_cmd.ranges.lin_vel_x = (0.1, 0.5)
twist_cmd.ranges.lin_vel_y = (-0.05, 0.05)
twist_cmd.ranges.ang_vel_z = (-0.05, 0.05)

######## refine phase 3: rear leg, 
We adjust and it was worst

########## refine phase 3: rear leg 2nd time:
# keep same stair height
step_height_range = (0.08, 0.12)

# slow and straight
twist_cmd.ranges.lin_vel_x = (0.08, 0.40)
twist_cmd.ranges.lin_vel_y = (0.0, 0.0)
twist_cmd.ranges.ang_vel_z = (0.0, 0.0)

# reward tuning
cfg.rewards["foot_clearance"].params["target_height"] = 0.14
cfg.rewards["foot_clearance"].weight = -0.7
cfg.rewards["foot_gait"].weight = 0.7
cfg.rewards["action_rate_l2"].weight = -0.06